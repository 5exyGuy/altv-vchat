{
    "$schema": "https://turborepo.org/schema.json",
    "pipeline": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["../../client/**", "../../server/**"]
        },
        "dev": {
            "dependsOn": ["^build"],
            "outputs": ["../../client/**", "../../server/**"]
        }
    }
}
